#!/usr/bin/env bash
# Disabled until the moment of correcting all the API files and reviewing the linter rules
# Run esLint
#eslint --ignore-pattern 'web-dapp/build/*' --ignore-pattern 'web-dapp/node_modules/*' --ignore-pattern 'web-dapp/public/*' --ignore-pattern 'web-dapp/src/*' web-dapp/;
#lintStatus=$?

#if [ $lintStatus -ne 0 ]; then
#  echo "Error Lint fail.";
#fi
#
#exit $lintStatus;

exit 0;